It's really common in React to put things into an array and display them on the dom

props allows us to pass from parent components to child components 

pure = dumb = function

impure = smart = class